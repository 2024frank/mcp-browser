{
  "name": "mcp-browser",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "npx @playwright/mcp --config playwright-mcp.config.json --port $PORT --host 0.0.0.0 --allowed-hosts '*' --browser chromium --headless --no-sandbox",
    "postinstall": "npx playwright install chromium"
  },
  "dependencies": {
    "@playwright/mcp": "^0.0.70",
    "playwright": "^1.42.0"
  }
}
